<p>Please select a location</p>

<div>
	<ul>
	<% @locations.each do |location| %>
	<%= link_to location.name, location_path(location) %>
	<% end %>
	</ul>
</div>
<%= link_to "Create a new location", new_admin_location_path %>